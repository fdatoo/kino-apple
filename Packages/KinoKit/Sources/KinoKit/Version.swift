/// Public namespace exposing KinoKit-wide constants such as the package version.
public enum KinoKit {
  /// Current KinoKit version string. Frozen at M3 close — the public surface
  /// shipped in M3 is the 1.0 baseline; further changes require a major bump.
  public static let version: String = "1.0.0"
}
