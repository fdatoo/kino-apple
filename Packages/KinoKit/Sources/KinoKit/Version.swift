/// Public namespace exposing KinoKit-wide constants such as the package version.
public enum KinoKit {
  /// Current KinoKit version string. Skeleton releases carry the `-skeleton`
  /// suffix until the real surface ships in M2.
  public static let version: String = "0.0.1-skeleton"
}
