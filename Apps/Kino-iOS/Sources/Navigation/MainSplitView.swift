import SwiftUI

/// iPad-only adaptive root. M3.8 implements the sidebar + detail panes.
struct MainSplitView: View {
  var body: some View {
    // Placeholder until M3.8 (iPad adaptation).
    MainTabView()
  }
}
